---
name: hardware-block-diagrams
description: 绘制、修改 CPU 微架构和 FPGA/GPU/NPU/SoC 硬件框图，表达模块层级、重复核阵列、共享存储、互连、并行数据通路与反馈，交付可编辑 draw.io、SVG、PNG。可从用户认可的图片或 PDF 学习具名风格，并从用户改图持续更新。适用于用户描述或上游 agent 提交的硬件结构，不用于普通流程图、电路原理图或物理版图。
---

# Hardware Block Diagrams

把硬件组成、共享关系和数据通路画清楚。主模型负责读图、理解硬件、设计构图和学习规则；脚本负责保存状态、检查结构和生成原生 draw.io。脚本不会自行理解图片、训练模型或证明 RTL 功能正确。

## 选择工作路线

- **画新图**：读 [硬件构图](references/hardware-grammar.md)、[输入与布局](references/interface-contract.md) 和 [命令](references/commands.md)。先检查本地风格，必要时按下面的联网审批流程补样本。
- **修改原图**：优先取得 `.drawio` 与硬件描述。导入后比较原始语义与编辑对象，不丢弃用户新增的图元。图像输入先视觉分析；不从模糊文字猜测端口或位宽。
- **学习／追加样本／收到用户改图**：读 [学习与更新](references/learning.md)。可以读 PNG/JPEG/SVG；PDF 先渲染相关页查看。用户原图、网络候选和生成结果有不同的入库规则。
- **选择或回退风格**：使用 library 命令；程序目录与用户风格库存储分离。不要在正常绘图时修改执行代码。

## 绘图工作流

1. 收集最少硬件事实：模块及父级、实例数量、共享资源的服务对象、连接端点与方向。用户自然语言和上游 agent JSON/YAML 都可用。具体端口、位宽、协议、时钟域有证据才填写。
2. 把自然语言规范化为 `hardware.json`。关键结构有歧义先询问；字体、间距、留白由你决定。参考图只提供画法，不提供新设计的模块、连接或容量。
3. 选择视图：`composition` 用于集群/SoC 组成；`microarchitecture` 用于并行执行、分发、汇聚和反馈。复杂设计可输出多个一致命名的视图。默认直接子模块一级，需要的局部再展开；不要因默认隐藏规则删掉用户要求的 FIFO、时钟、复位或控制线。
4. 解析本次风格：明确要求优先，其次指定风格、用户默认风格、与视图相符的内置样式。读取当前有效版本及其批准样本，实际查看最相关样本。显式指定不存在的风格时说明缺失，不能偷偷换用默认。
5. **布局先于布线**。复杂连接或用户反馈杂乱时，先读 [布局设计与候选比较](references/placement.md)，依据连接关系重新编排模块。已有坐标只是候选，除非用户锁定，允许移动模块、改变大小/长宽比、交换区域顺序、调整接口侧边与容器尺寸；保留真实层级和所有连接。先安排强关联邻居、并行通路与共享资源的位置，再定端点和走廊。不要默认把同类模块集中一列，也不要把所有硬件转为顺序流程图。
6. 为复杂图先比较两个有实质位置差异的构图，简单图无需增加步骤。编写各自的 `layout.json`，再按 [连线整理](references/routing.md) 处理直连与正交避让。routing.py 只是固定位置后的可选工具；如果结果出现跨区长回路、成束交叉或多余折返，返回调整模块，不能只反复增大留白/均分端口/重新布线。同侧端点尽量成组居中等距，但不能为了全边机械均分制造绕路。
7. 验证后用 scripts/diagram.py 生成原生 `.drawio` 并由 draw.io Desktop 导出 SVG/PNG。查看实际 PNG，检查硬件关系、模块排布、交叉、文字、箭头和反馈。用 compare_layouts.py 辅助对比位置变化和走线代价；几何分数不是视觉通过。折点/框位置改变后重新定位标签。若某区域仍乱，优先重排该区域；只在完整视图确实不可读时再补局部视图，不擅自删除原图信号。报告仍存在的问题与实际成功格式。
8. 交付 `.drawio`、SVG、PNG，以及可用于后续修改的 hardware/layout/manifest。给出预览与本地链接。图完成不代表用户已经认可画风。

## 联网与学习授权

这是用户明确要求的两道审批，不得用 skill 自行省略：

1. 先判断本地样本的具体缺口，说明拟查内容，取得用户**联网搜索**批准后才搜索。
2. 搜索后展示真实图的缩略图、原始来源、页码/图号和参考理由，取得用户对具体样本的**学习入库**批准后才学习。

搜索许可不等于样本许可。待审/拒绝样本不能影响本次绘图或个人风格。无网或拒绝联网时可用本地批准资料画图，说明缺口，不虚构来源。网络样本不得覆盖个人默认风格，除非用户要求。

用户明确提供“这是我认可的硬件框图／学习此图”的原图可以登记为已授权参考。用户明确返回针对前次结果修改后的图时，按既定反馈授权自动提炼**可识别的画法差异**，保存对应具名风格的新版本并报告学到什么。若无法判断它是改图还是普通附件，或无法关联风格，先询问。仅模型生成的图不能自动成为学习样本。

## 不可丢失的硬件语义

- 包含关系、连接关系和资源共享是不同关系。不能用包围盒暗示不存在的归属，也不能从 `shared_with` 自动生成未知连接。
- 同一核的多个实例要保持一致布局；需要逐实例连接时显式列出实例。`count` 只标明聚合数量，不自动展开。
- 支持宽带箭头、细控制线和双向接口；粗线表示视觉类别，不自动等于确切位宽。双向协议不应被当成单向流水线。
- 指向模块边界的端点必须保持绑定。折线通过明确布线路径控制；不要为了好看变成浮动装饰箭头。
- 参考图的颜色不自动解释成时钟域；图形面积不自动解释成物理面积。缺少图例时记录解释不确定性。

## 环境与来源

Python 3.10+（本机用 Kimi 托管 Python 即可，`python` 命令直接可用）；JSON 路线仅需标准库，YAML 需要 PyYAML。图片/PDF 查看直接用 Kimi 自身的媒体阅读能力，PDF 也可用托管环境的 pypdfium2 渲染；只有确需脚本化处理图片时才安装 Pillow/PyMuPDF（见 requirements-optional.txt）。draw.io Desktop 用于真实 SVG/PNG 导出：本机已便携安装于 `E:\KimiData\Tools\draw.io\draw.io.exe`，脚本自动探测；也可用环境变量 `DRAWIO_DESKTOP` 或 `--drawio` 指定。风格库默认 `~/.kimi/hardware-block-diagrams/`，本机通过环境变量 `HBD_LIBRARY` 指向 `E:\KimiData\kimi\hardware-block-diagrams`。先检测依赖，不自动安装不相关应用或修改全局配置。该 skill 无必需 MCP、云服务或后台抓取。

改造依据与许可证见 [来源](references/upstream.md)。内置两种样式是从用户明确认可的构图提炼的起点，不宣称适用于所有硬件图；个人样本保存在用户库，不随可分享的 skill 包分发。
